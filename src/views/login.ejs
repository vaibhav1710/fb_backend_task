<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet"> <!-- Include Tailwind CSS CDN -->
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
        <h1 class="text-xl font-bold text-center mb-4">Login</h1>
        <form action="/login" method="POST">
           
            <% if (error) { %>
                <div class="bg-red-500 text-white p-4 rounded mb-4 text-center">
                    <p><%= error %></p>
                </div>
            <% } %>
    

            <label class="block text-gray-700 text-sm font-bold mb-2" for="email">Email:</label>
            <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline mb-4" type="email" id="email" name="email" required>
            
            <label class="block text-gray-700 text-sm font-bold mb-2" for="phoneNumber">Phone Number:</label>
            <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline mb-4" type="text" id="phoneNumber" name="phoneNumber" required pattern="\d{10}" maxlength="10">
            
            
            <div class="flex justify-center">
                <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" type="submit">Login</button>
            </div>
        </form>
        <div class="flex justify-center mt-8">
            <a href="/feed" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
               Back to Feed
            </a>
        </div>
    </div>
</body>
</html>
